module diabatic_all

    use parameter, only : rkappa, cp
    use biseki, only : biseki_biseki

    implicit none

    private
    public :: 

    contains


    !function Exner(Numerator, Denominator) result(output)
    !    real(4), intent(in) :: Numerator
    !    real(4), intent(in) :: Denominator
    !    real(4) :: output

    !    output = 


end module diabatic_all
